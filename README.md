# sportschau-liveticker2csv
Sportschau Liveticker parser - all live entrys will be written in a csv file

## Basic usage

``` python
liveticer2csv.py --url <url to liveticker> --data-dir <relative or absolute path to data directory>
```
